# IMPORTANT: To ensure we're not accidentally committing autogenerated mocks,
#            make sure to prefix all filenames with `mock_`

issue-845-fix: true
packages:
  trenchcoat/internal/handlers:
    interfaces:
      AuthServiceInterface:
        config:
          with-expecter: true
          filename: mock_auth_test.go
          outpkg: handlers_test
          dir: ./internal/handlers
