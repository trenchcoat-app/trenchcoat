# IMPORTANT: To ensure we're not accidentally committing autogenerated mocks,
#            make sure to prefix all filenames with `mock_`

issue-845-fix: true
packages:
  trenchcoat/internal/interfaces:
    interfaces:
      AuthServiceInterface:
        config:
          with-expecter: true
          filename: mock_auth.go
          outpkg: mocks
          dir: ./internal/interfaces/mocks
