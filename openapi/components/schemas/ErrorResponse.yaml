type: object
required:
  - code
  - message
properties:
  code:
    type: string
    description: "A machine-readable error code identifying the type of error (e.g., 'EMAIL_ALREADY_EXISTS', 'VALIDATION_FAILED')."
  message:
    type: string
    description: "A human-readable description of the error."
  details:
    type: array
    items:
      $ref: "./ErrorResponseDetail.yaml"
