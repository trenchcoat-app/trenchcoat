type: object
required:
  - name
  - amount
  - splits
properties:
  name:
    type: string
  amount:
    type: integer
    description: Amount in the smallest currency unit (e.g. cents).
  splits:
    type: array
    items:
      $ref: "./CreateTransactionItemSplit.yaml"