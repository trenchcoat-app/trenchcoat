type: object
required:
  - funderId
  - amount
properties:
  funderId:
    type: string
    format: uuid
    description: "Account ID of the user who contributed to the payment of the transaction."
  amount:
    type: integer
    description: Amount in the smallest currency unit (e.g. cents).