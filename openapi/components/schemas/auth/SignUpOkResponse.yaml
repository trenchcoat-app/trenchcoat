type: object
required:
  - user
properties:
  session:
    description: "The newly created authentication session. Only returned if autoSignIn was set to true in the request body."
    $ref: "./Session.yaml"
  user:
    description: "The newly created user."
    $ref: "./User.yaml"
