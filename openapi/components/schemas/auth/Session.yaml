type: object
required:
  - expiresAt
  - accountId
properties:
  expiresAt:
    type: string
    format: date-time
    description: Session expiration timestamp.
  accountId:
    type: string
    format: uuid
